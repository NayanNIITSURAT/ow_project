enum CommentableType { LISTING, STORY, REEL }
enum ChatableType { LISTING, STORY, USER, MEDIA, LOCATION }
enum LikeableType { LISTING, STORY, REEL, COMMENT }
enum ViewableType { LISTING, STORY, REEL, PROFILE }
enum StoryType { IMAGE, VIDEO, TEXT, NULL }
enum LoadState { loading, success, failure }
enum PlaybackState { pause, play, next, previous }
enum SwipeAction { next, previous }
enum ChatState { SNEDING, SENT, DELIVERED, SEEN, FAILED }
enum MediaType { IMAGE, VIDEO, AUDIO, DOC }
